<%= form_for(@howner) do |f| %>
  <% if @howner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@howner.errors.count, "error") %> prohibited this howner from being saved:</h2>

      <ul>
      <% @howner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="field">
    <%= f.label :owner %><br />
    <%= f.text_field :owner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
